<chapter id="jboss.config">
	<title>JBoss Configuration and Launch</title>
	<para>
		Now, it is time to configure the JBoss server if it has not been done yet.
	</para>

	<informaltable frame="none">
	<tgroup cols="2" align="left" colsep="1" rowsep="1">
		<tbody>
			<row>
				<entry>
					<para>Click on the debug shortcut and select <literal>Debug…</literal> to open the debug configurations.</para>
				</entry>
				<entry>
		            <mediaobject>
		                <imageobject>
		                    <imagedata align="center" fileref="images/debug-menu.jpg"/>
		                </imageobject>
		            </mediaobject>
				</entry>
			</row>
			<row>
				<entry>
					<para>The debug dialog allows you to configure the available JBoss configurations that will be used for debugging.</para>
				</entry>
				<entry>
		            <mediaobject>
		                <imageobject>
		                    <imagedata align="center" fileref="images/jboss-launch-config-1.jpg"/>
		                </imageobject>
		            </mediaobject>
				</entry>
			</row>
			<row>
				<entry>
					<para>In order to view source code when debugging, you must include the project in the source lookup path;  otherwise Eclipse will complain that it cannot locate the source.</para>
					<para>
						To specifiy a source lookup path, go into the JBoss launch configuration and select the <literal>Source</literal> tab.
						Click on the <literal>Add</literal> button and select <literal>Java Project</literal>. Select your project and click <literal>OK</literal>.</para>
					<para>NB : Older version of JBoss Eclipse IDE added all opened projects to the source lookup path, but it lead to source conflicts and was removed.</para>
				</entry>
				<entry>
		            <mediaobject>
		                <imageobject>
		                    <imagedata align="center" fileref="images/jboss-launch-config-2.jpg"/>
		                </imageobject>
		            </mediaobject>
				</entry>
			</row>
			<row>
				<entry>
					Select the configuration you want to launch and click on <literal>Debug</literal> and you will see JBoss starting. The output is sent to the console.
				</entry>
				<entry>
					<mediaobject>
		                <imageobject>
		                    <imagedata align="center" fileref="images/jboss-run.jpg"/>
		                </imageobject>
		            </mediaobject>
				</entry>
			</row>
		</tbody>
	</tgroup>
	</informaltable>

</chapter>