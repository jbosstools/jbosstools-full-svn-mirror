The feature and plugin in this folder can be built by running `mvn clean install` in this folder:

https://svn.jboss.org/repos/devstudio/trunk/extras/

The resulting files will be generated in ./extras/site/target/site/{features,plugins}/

