To easily run the build*.xml mirror scripts, ssh to hudson@qa01, then run:

cd ~/RHDS/updates/requirements/webtools; mirror.sh build-indigo.xml

mirror.sh script is also in this folder for reference

