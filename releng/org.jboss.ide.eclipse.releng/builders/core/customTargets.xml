<project name="Build specific targets and properties" default="noDefault" >
	<taskdef name="updateVersions" classname="org.jboss.ide.eclipse.releng.UpdateVersionsTask" classpath="../../bin"/>
	
	<taskdef classpath="../../lib/ant-contrib.jar" resource="net/sf/antcontrib/antlib.xml"/>
	<import file="../common/bundlePlugins.xml"/>
	
<!-- ===================================================================== -->
<!-- Run a given ${target} on all elements being built -->
<!-- Add on <ant> task for each top level element being built. -->
<!-- ===================================================================== -->
<target name="allElements">
	<ant antfile="${genericTargets}" target="${target}" >
		<property name="type" value="feature" />
		<property name="id" value="org.jboss.ide.eclipse.feature" />
	</ant>
</target>

<!-- ===================================================================== -->
<!-- Targets to assemble the built elements for particular configurations  -->
<!-- These generally call the generated assemble scripts (named in -->
<!-- ${assembleScriptName}) but may also add pre and post processing -->
<!-- Add one target for each root element and each configuration -->
<!-- ===================================================================== -->

<target name="assemble.org.jboss.ide.eclipse.feature">
	<property name="archiveName" value="JBossIDE-CORE-${versionTag}.zip"/>
	<ant antfile="${assembleScriptName}" dir="${buildDirectory}"/>
</target>

<!-- ===================================================================== -->
<!-- Check out map files from correct repository -->
<!-- Replace values for cvsRoot, package and mapVersionTag as desired. -->
<!-- ===================================================================== -->
<target name="getMapFiles">
	<!-- Notify recipients that build has started.-->
	<!-- In real life we'll want to commit map files to CVS <property name="cvsRoot" value=":pserver:anonymous@cvs.sourceforge.net:/cvsroot/jboss/jbosside/core" />
	<property name="mapVersionTag" value="HEAD" />
	<cvs 
		package="${cvsRoot}"
		dest="${buildDirectory}/maps"
		tag="${mapVersionTag}"
		/> -->

	<copy file="${build.root}/builders/core/jbosside-core.map" todir="${buildDirectory}/maps"/>
</target>

<!-- ===================================================================== -->
<!-- Steps to do before setup -->
<!-- ===================================================================== -->
<target name="preSetup">
	<!-- clean up the build directory -->
	<!--delete failonerror="true" includeemptydirs="true">
		<fileset dir="${buildDirectory}" defaultexcludes="false"/>
	</delete-->
</target>

<!-- ===================================================================== -->
<!-- Steps to do after setup but before starting the build proper -->
<!-- ===================================================================== -->
<target name="postSetup">
</target>

<!-- ===================================================================== -->
<!-- Steps to do before fetching the build elements -->
<!-- ===================================================================== -->
<target name="preFetch">
</target>

<!-- ===================================================================== -->
<!-- Steps to do after fetching the build elements -->
<!-- ===================================================================== -->
<target name="postFetch">
	<condition property="append" else="false">
		<not>
			<equals arg1="${buildType}" arg2="R"/>
		</not>
	</condition>
	<updateVersions type="feature" version="${versionTag}" append="${append}"
		updateAssembleScript="false" recursivePlugins="true"
		pluginRegex="org.jboss.ide.*">
		<fileset dir="${buildDirectory}/features">
			<include name="*org.jboss.ide*"/>
			<exclude name="*aop*"/>
			<exclude name="*ejb3*"/>
		</fileset>
	</updateVersions>
</target>

<!-- ===================================================================== -->
<!-- Steps to do before generating the build scripts. -->
<!-- ===================================================================== -->
<target name="preGenerate">
</target>

<!-- ===================================================================== -->
<!-- Steps to do after generating the build scripts. -->
<!-- ===================================================================== -->
<target name="postGenerate">
</target>


<!-- ===================================================================== -->
<!-- Steps to do before running the build.xmls for the elements being built. -->
<!-- ===================================================================== -->
<target name="preProcess">
</target>

<!-- ===================================================================== -->
<!-- Steps to do after running the build.xmls for the elements being built. -->
<!-- ===================================================================== -->
<target name="postProcess">
</target>


<!-- ===================================================================== -->
<!-- Steps to do before running assemble. -->
<!-- ===================================================================== -->
<target name="preAssemble">
</target>

<!-- ===================================================================== -->
<!-- Steps to do after  running assemble. -->
<!-- ===================================================================== -->
<target name="postAssemble">
	
	<bundlePlugins pluginList="${include.plugins}" buildDirectory="${buildDirectory}"
		archivePath="${buildDirectory}/${buildLabel}/JBossIDE-CORE-${versionTag}.zip" />
	
	<!--property name="tmp.dir" value="${buildDirectory}/../tmp"/>
	<property name="plugins.dir" value="${buildDirectory}/plugins"/>
		
	<mkdir dir="${tmp.dir}/plugins" />
		
	<for list="${include.plugins}"  param="plugin.name" trim="true">
		<sequential>
			<copy todir="${tmp.dir}/plugins">
				<fileset dir="${plugins.dir}">
					<include name="@{plugin.name}_*/**/*.*" />
					<include name="@{plugin.name}_*.jar"/>
				</fileset>
			</copy>
		</sequential>
	</for>
	
	<property name="archive" value="${buildDirectory}/${buildLabel}/JBossIDE-CORE-${versionTag}.zip"/>
	<zip destfile="${archive}" basedir="${tmp.dir}" update="yes"/>
	<delete dir="${tmp.dir}"/-->
</target>

<!-- ===================================================================== -->
<!-- Steps to do after the build is done. -->
<!-- ===================================================================== -->
<target name="postBuild">
	<!-- upload the build -->
	<!--property name="archiveName" value="JBossIDE-CORE-${versionTag}.zip"/>
	<property name="archiveFullPath" value="${buildDirectory}/${buildLabel}/${archiveName}"/>
	<echo>uploading ${archiveFullPath} into ${ftpBaseDir}/${uploadBuildDirectory}...</echo>
	<ftp server="${ftpServer}"
		userid="${ftpUser}"
		password="${ftpPassword}"
		remotedir="${ftpBaseDir}/${uploadBuildDirectory}">

		<fileset file="${archiveFullPath}"/>
	</ftp-->	
</target>

<!-- ===================================================================== -->
<!-- Steps to do to test the build results -->
<!-- ===================================================================== -->
<target name="test">
</target>

<!-- ===================================================================== -->
<!-- Steps to do to publish the build results -->
<!-- ===================================================================== -->
<target name="publish">
</target>

<!-- ===================================================================== -->
<!-- Default target                                                        -->
<!-- ===================================================================== -->
<target name="noDefault">
	<echo message="You must specify a target when invoking this file" />
</target>

</project>

