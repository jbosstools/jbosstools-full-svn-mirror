<project name="Build specific targets and properties" default="noDefault" >
	<taskdef name="updateVersions" classname="org.jboss.ide.eclipse.releng.UpdateVersionsTask" classpath="../../bin"/>
	
	<taskdef classpath="../../lib/ant-contrib.jar" resource="net/sf/antcontrib/antlib.xml"/>
	
<!-- ===================================================================== -->
<!-- Run a given ${target} on all elements being built -->
<!-- Add on <ant> task for each top level element being built. -->
<!-- ===================================================================== -->
<target name="allElements">
	<ant antfile="${genericTargets}" target="${target}" >
		<property name="type" value="feature" />
		<property name="id" value="org.hibernate.eclipse.feature" />
	</ant>
</target>

<!-- ===================================================================== -->
<!-- Targets to assemble the built elements for particular configurations  -->
<!-- These generally call the generated assemble scripts (named in -->
<!-- ${assembleScriptName}) but may also add pre and post processing -->
<!-- Add one target for each root element and each configuration -->
<!-- ===================================================================== -->

<target name="assemble.org.hibernate.eclipse.feature">
        <dirname property="bdir" file="${assembleScriptName}" />
        <echo message="builder is: ${buildDirectory}"/>
		  <echo message="file ${assembleScriptName} is in ${bdir}"/>

		  <property name="archiveName" value="HibernateTools-${hibernate.tools.release}.${versionTag}.zip"/>
		  <ant antfile="${assembleScriptName}"  dir="${buildDirectory}"/>
</target>

<!-- ===================================================================== -->
<!-- Check out map files from correct repository -->
<!-- Replace values for cvsRoot, package and mapVersionTag as desired. -->
<!-- ===================================================================== -->
<target name="getMapFiles">
	 <echo message="TODO: maybe store map into cvs, then retrieve here"/>
	  <mkdir dir="${buildDirectory}/maps" />
	  <copy file="${build.root}/builders/hibernate-tools/maps/hibernate.map"
	     todir="${buildDirectory}/maps"/>
</target>

<!-- ===================================================================== -->
<!-- Steps to do before setup -->
<!-- ===================================================================== -->
<target name="preSetup">
</target>

<!-- ===================================================================== -->
<!-- Steps to do after setup but before starting the build proper -->
<!-- ===================================================================== -->
<target name="postSetup">
</target>

<!-- ===================================================================== -->
<!-- Steps to do before fetching the build elements -->
<!-- ===================================================================== -->
<target name="preFetch">
</target>

<!-- ===================================================================== -->
<!-- Steps to do after fetching the build elements -->
<!-- ===================================================================== -->
<target name="postFetch">

   <property name="tools.dist.dir" location="${hibernate-ext-src.home}/tools/target/hibernate-tools/" />
   <property name="console.dist.dir" location="${hibernate-ext-src.home}/console/target/hibernateconsole" />

   <!--delete dir="${buildDirectory}/plugins/org.hibernate.eclipse/lib"> 
   	 <include name="**/*"/>
   </delete-->
	
	<!--ant antfile="${hibernate-ext-src.home}/org.hibernate.eclipse/hibernate-build.xml" dir="${hibernate-ext-src.home}/org.hibernate.eclipse" target="update"/-->
	<!--echo>${hibernate-ext-src.home}</echo>
	<exec executable="${hibernate-ext-src.home}/build.${buildScriptExt}" dir="${hibernate-ext-src.home}/org.hibernate.eclipse">
		<arg line="-f hibernate-build.xml"/>
		<arg line="update"/>
	</exec>
   <copy todir="${buildDirectory}/plugins/org.hibernate.eclipse/lib" verbose="true">
     <fileset dir="${hibernate-ext-src.home}/org.hibernate.eclipse/lib">
     	<include name="**/*"/>
	 </fileset>
   </copy-->

	<updateVersions type="feature" version="${hibernate.tools.release}.${versionTag}" append="false"
		updateAssembleScript="false" recursivePlugins="true" failOnRecursiveError="false"
		pluginRegex="org.hibernate.*"
		>
		
		<fileset dir="${buildDirectory}/features">
			<include name="*hibernate*"/>
		</fileset>
	</updateVersions>
	
</target>

<!-- ===================================================================== -->
<!-- Steps to do before generating the build scripts. -->
<!-- ===================================================================== -->
<target name="preGenerate">
</target>

<!-- ===================================================================== -->
<!-- Steps to do after generating the build scripts. -->
<!-- ===================================================================== -->
<target name="postGenerate">
</target>


<!-- ===================================================================== -->
<!-- Steps to do before running the build.xmls for the elements being built. -->
<!-- ===================================================================== -->
<target name="preProcess">
  	<!-- Update the versions in all features -->
	<!--updateVersions type="feature" version="${versionTag}" append="true" pluginRegex="org\.hibernate\..*">
		<fileset dir="${buildDirectory}/features">
			<include name="*hibernate*"/>
		</fileset>
	</updateVersions-->
	
	<!-- Update the versions in all plugins (including META-INF/MANIFEST.MF and plugin.xml) -->
	<!--updateVersions type="plugin" version="${versionTag}" append="true">
		<fileset dir="${buildDirectory}/plugins">
			<include name="*hibernate*"/>
		</fileset>
	</updateVersions-->
</target>

<!-- ===================================================================== -->
<!-- Steps to do after running the build.xmls for the elements being built. -->
<!-- ===================================================================== -->
<target name="postProcess">
</target>


<!-- ===================================================================== -->
<!-- Steps to do before running assemble. -->
<!-- ===================================================================== -->
<target name="preAssemble">
</target>

<!-- ===================================================================== -->
<!-- Steps to do after  running assemble. -->
<!-- ===================================================================== -->
<target name="postAssemble">
</target>

<!-- ===================================================================== -->
<!-- Steps to do after the build is done. -->
<!-- ===================================================================== -->
<target name="postBuild">
</target>

<!-- ===================================================================== -->
<!-- Steps to do to test the build results -->
<!-- ===================================================================== -->
<target name="test">
</target>

<!-- ===================================================================== -->
<!-- Steps to do to publish the build results -->
<!-- ===================================================================== -->
<target name="publish">
</target>

<!-- ===================================================================== -->
<!-- Default target                                                        -->
<!-- ===================================================================== -->
<target name="noDefault">
	<echo message="You must specify a target when invoking this file" />
</target>

</project>
