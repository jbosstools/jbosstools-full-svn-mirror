<project default="build.requirement">
	<import file="../generic/build.xml" />

	<target name="unpack-zip" >
		<unzip src="${driver.dest}/${build.archive}" dest="${unzip.dest}" >
			<patternset>
				<include name="${build.archive.root}/seam-gen/**" />
				<include name="${build.archive.root}/seam" />				
				<include name="${build.archive.root}/lib/*.jar" />
				<include name="${build.archive.root}/lib/test/*.jar" />
				<include name="${build.archive.root}/bootstrap/**" />
			</patternset>
		</unzip>
	</target>

</project>
